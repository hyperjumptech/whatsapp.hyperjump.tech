# Comma separated app's name to enable debugging. Leave empty if not needed
DEBUG=web,facebook-webhooks

# For docker postgres initialization
POSTGRES_USER=root
POSTGRES_PASSWORD=root

# The URL of the Postgres database pool to store user data
DATABASE_URL="postgresql://postgres:root@localhost:5432/postgres?schema=public" #required

# The URL of the direct Postgres database. Used for Prisma migrations
DATABASE_DIRECT_URL="postgresql://postgres:root@localhost:5432/postgres"

# The base URL of the whatsapp.hyperjump.tech
NEXT_PUBLIC_BASE_URL=http://localhost:3000 #required

# The base URL of the monika notify URL that user can use in their monika configuration. In production, set this to the URL of the deployed `notify-api` server.
NEXT_PUBLIC_MONIKA_NOTIFY_API_URL=http://localhost:5001

# The base URL of the facebook whatsapp api. In production, set this to the URL that facebook provides, e.g., https://graph.facebook.com/v15.0 
WHATSAPP_API_BASE_URL=http://localhost:5001 #required

# The Whatsapp api phone id
WHATSAPP_API_PHONE_ID='123123213123' #required

# The access token
WHATSAPP_API_ACCESS_TOKEN='ASDFASDFASDF' #required

# The URL of docs using Monika with Whatsapp
NEXT_PUBLIC_MONIKA_NOTIF_DOC="https://monika.hyperjump.tech/guides/notifications#whatsapp" #required

# The secret to use to check the signature of the request sent by facebook to us
FACEBOOK_WEBHOOK_APP_SECRET=your-facebook-app-secret-here #required

# The token to verify the GET request from facebook webhook request
FACEBOOK_WEBHOOK_VERIFY_TOKEN=abcd #required

# The number of seconds before user can ask for resending instruction
NEXT_PUBLIC_WEBHOOK_RESEND_COOLDOWN_TIME= #number #in seconds